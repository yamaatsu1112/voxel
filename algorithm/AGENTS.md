# Codex Notes

- Commands or tests that require a GPU cannot access the GPU inside the sandbox. Request escalated permissions before running them.
- Prefer the cleanest design even when it requires breaking compatibility; do not preserve compatibility at the cost of clarity or correctness.
