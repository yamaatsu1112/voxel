---
name: checking-implementation
description: Check the codes after implementaion. Use when finished implementation.
allowed-tools: Bash(cargo build:*), Bash(cargo check:*)
---
## Overview
1. Check the compilation error.
  - `cargo build`, `cargo check`
2. Run the codes.
  - Use `running-code` skill.